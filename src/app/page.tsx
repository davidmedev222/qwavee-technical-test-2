import { FavoriteGamesSection, HeroSection } from '@/components'

function HomePage() {
  return (
    <main>
      <HeroSection />
      <FavoriteGamesSection />
    </main>
  )
}

export default HomePage
